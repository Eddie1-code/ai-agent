[project]
name = "mcp-python-tools"
version = "0.1.0"
description = "Python MCP Server for 知域Agent - 提供数据分析与文本处理工具"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "pandas>=2.2.0",
    "httpx>=0.27.0",
]

[project.scripts]
mcp-python-tools = "server:main"
